import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './QuickNote.styles';

const QuickNote = (props) => (
  <div className="QuickNoteWrapper">
    Test content
  </div>
);

QuickNote.propTypes = {
  // bla: PropTypes.string,
};

QuickNote.defaultProps = {
  // bla: 'test',
};

export default QuickNote;
