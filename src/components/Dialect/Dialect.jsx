import React from 'react';
import PropTypes from 'prop-types';
//import { Test } from './Dialect.styles';

const Dialect = (props) => (
  <div className="DialectWrapper">
    Test content
  </div>
);

Dialect.propTypes = {
  // bla: PropTypes.string,
};

Dialect.defaultProps = {
  // bla: 'test',
};

export default Dialect;
